import * as h from '../helpers';
import f from '../index';

describe('Array field', () => {
  it('Simple construct', (done) => {
    f.array(f.bool());
    done();
  });
  it('Construct with value');
});
